machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install redis-server
  override:
    - pip install --upgrade -r requirements.txt
    - pip install -e .

test:
  pre:
    - ls
  override:
    - py.test -v
